class Solution {
	int size=0;
    public int solution(int[] A) {
    	// Starting with mid element
    	// Assume that Size of the array as the start point
    	this.size=A.length;
    	int sum_prev =0;
    	int sum_next =0;
    	boolean status = true;
    	while(status){
    		for(int i = )
    		
    	}
		return 0;
        // write your code in Java SE 8
    }
    public int rand(){
    	random.nextInt(this.size - 0);
    	return 0;
    }
}